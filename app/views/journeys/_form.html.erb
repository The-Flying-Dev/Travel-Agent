<%= form_with(model: journey) do |form| %>
  <% if journey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(journey.errors.count, "error") %> prohibited this journey from being saved:</h2>

      <ul>
        <% journey.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :train_id %>
    <%#= form.text_field :train_id %>     
    <%= form.collection_radio_buttons(:train_id, @trains,:id,:name ) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
